#!/bin/sh
#
# Enables the `syncearly` patch within the VDR Extension patch
#
# Start live display as soon as possible, not waiting for sync of audio and video
#
. debian/patches/extension-patch-helper.sh

toggle_extension_patch SYNCEARLY $1

exit 0
