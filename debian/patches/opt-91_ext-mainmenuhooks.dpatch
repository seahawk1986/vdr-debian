#!/bin/sh
#
# Enables the `mainmenuhooks` patch within the VDR Extension patch
#
# Allows to replace main menu entries by some special plugins (like epgsearch, extrecmenu, ...)
#
. debian/patches/extension-patch-helper.sh

toggle_extension_patch MAINMENUHOOKS $1

exit 0
