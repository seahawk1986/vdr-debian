#!/bin/sh
#
# Enables the `pluginapi` patch within the VDR Extension patch
#
# Autopatch Plugin-Makefiles (API)
#
. debian/patches/extension-patch-helper.sh

toggle_extension_patch PLUGINAPI $1

exit 0
