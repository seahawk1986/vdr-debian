#!/bin/sh
#
# Enables the `sourcecaps` patch within the VDR Extension patch
#
# Allows to define capabilities of dvb-cards (e.g. card1 can receive Sat @28.2E)
#
. debian/patches/extension-patch-helper.sh

toggle_extension_patch SOURCECAPS $1

exit 0
