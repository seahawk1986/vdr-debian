#!/bin/sh
#
# Enables the `cutterqueue` patch within the VDR Extension patch
#
# Adds a queue of recordings to be cutted
#
. debian/patches/extension-patch-helper.sh

toggle_extension_patch CUTTERQUEUE $1

exit 0
